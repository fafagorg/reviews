'use strict'

module.exports.findReviewByid = function findReviewByid(req, res, next) {
  res.send({
    message: 'This is the mockup controller for findReviewByid'
  });
};

module.exports.deleteReview = function deleteReview(req, res, next) {
  res.send({
    message: 'This is the mockup controller for deleteReview'
  });
};

module.exports.updateReview = function updateReview(req, res, next) {
  res.send({
    message: 'This is the mockup controller for updateReview'
  });
};